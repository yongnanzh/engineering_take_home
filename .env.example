# Example environment variables for local development.
# Copy this file to `.env` and replace placeholder values with real credentials.

DB_USERNAME=app
DB_PASSWORD=takehome

RAILS_MAX_THREADS=5

ENGINEERING_TAKE_HOME_DATABASE_PASSWORD=production_db_password_here

# DB host/port defaults to Docker Compose service `db` and 5432.
# If running Postgres locally, uncomment and set these values as needed.
# DB_HOST=db
# DB_PORT=5432
